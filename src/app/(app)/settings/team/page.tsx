export const dynamic = "force-dynamic";

export default function TeamSettingsPage() {
  return (
    <p className="text-sm text-muted-foreground">
      Equipo — en construcción (US7)
    </p>
  );
}
